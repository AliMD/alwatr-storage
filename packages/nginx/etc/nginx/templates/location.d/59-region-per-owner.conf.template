# PerOwner region
# Nitrobase file location specific to each owner id.

location ~ ^/$nitrobaseRegionPerOwner/[A-z0-9_=-]+/[A-z0-9_=-]+/ {
  set $debugInfo 59;

  if ($authUserToken = "") {
    # User not defined
    set $debugInfo 591;
    return 403;
    break;
  }

  try_files $uri =404;
}
