# Public region
# Nitrobase file location that can be accessed by anyone.

location /$nitrobaseRegionPublic/ {
  if ($authUserToken = "") {
    # User not defined
    return 403;
    break;
  }

  try_files $uri =404;
}
