# Public region
# Nitrobase file location that can be accessed by anyone.

location /$nitrobaseRegionPublic/ {
  set $debugInfo 55;

  if ($authUserToken = "") {
    set $debugInfo 551;
    # User not defined
    return 403;
    break;
  }

  try_files $uri =404;
}
