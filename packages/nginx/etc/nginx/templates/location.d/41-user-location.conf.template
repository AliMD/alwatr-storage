# lower $userIdPrefixLower $userIdPrefix

# Extracting user location from the authUserId and userIdPrefix and converting userIdPrefix to lowercase
set $userLocation "$nitrobaseRegionPerUser/$userIdPrefix/$authUserId";
